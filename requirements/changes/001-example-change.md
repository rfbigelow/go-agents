# [Title]

<!-- A change request captures a requirement change from ask through
     analysis to implementation in the PEGS files. One change request
     maps to one PR. When merged, the PEGS files reflect the updated
     requirements and this file preserves the reasoning.

     The first change request (001-initial-requirements) is typically
     the initial elicitation that populates the PEGS files from the
     empty templates. Subsequent ones modify existing content. -->

## Ask

<!-- What was requested, in the stakeholder's terms. What prompted this
     change? This is the "raw" ask before elicitation and analysis. -->

## Analysis

<!-- What does this ask mean for the requirements? What did elicitation
     reveal? What decisions were made and why? This section captures the
     reasoning that isn't visible in the PEGS file diffs alone. -->

## PEGS Impact

<!-- Which specific PEGS files and sections were added or modified.
     Use the prefix shorthand.

     e.g.,
     - e2: added TenantService external component
     - e3: added data residency constraint (E3.4)
     - s1: added TenantManager component (S1.5)
     - s2: added tenant CRUD operations (S2.12–S2.15)
     - s6: added tenant isolation verification (S6.8) -->
